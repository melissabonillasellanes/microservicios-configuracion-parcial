
#spring:
#  mongodb:
#    embedded:
#      version: 4.0.2
#  data:
#    mongodb:
#      database: test
#      port: 27017
#server:
#  port: 0

server:
  port: '${PORT:${SERVER_PORT:0}}'
  spring:
    data:
      mongodb: { host: localhost, port: 27017, database: test, username: root, password: serra2022 }
  application:
    name: serie-service
  eureka:
    client:
      register-with-eureka: true
      fetch-registry: true
      registry-fetch-interval-seconds: 5
      healthcheck: { enabled: true }
      service-url: { default-zone: 'http://localhost:8761/eureka' }
  instance:
    hostname: localhost
    instance-id: '${spring.application.name}:${spring.application.instance_id:${random.value}}'
    prefer-ip-address: true